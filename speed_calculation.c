#include <stdio.h>
#include <math.h>
#define G 32.17;

double theta(double x){
	return 
}

double userInput(char str[]) {
	double input;
	printf(str);
	scanf("%lf", &input);
	return input;
}

int main(){
	double theta, distance, velocity;
	theta = userInput("Input for theta>");
	distance = userInput("Input for distance>");
	velocity = userInput("Input for velocity>");
}